CREATE TABLE DBSHARES (
    ID VARCHAR(36) PRIMARY KEY NOT NULL,
    NAME VARCHAR(64) NOT NULL,
    PATH VARCHAR(256),
    TYPE VARCHAR(32),
    SIZE VARCHAR(256),
    CLIENTS VARCHAR(256),
    OPTIONS VARCHAR(256),
    STATUS VARCHAR(1024)
);
